# URL del backend de voz (voicecall/agent). En dev local: http://localhost:8000
AGENT_URL=http://localhost:8000
# Mismo token que AGENT_API_TOKEN en el .env del agente (solo server-side, nunca llega al browser)
AGENT_API_TOKEN=elegi-otro-token-secreto
